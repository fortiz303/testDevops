#Storing our users input to input_count
input_count = input('Input any word:')

#Counting how many a are in the input using the count method 
print("the letter 'a' appears {} times".format(input_count.count('a')))
